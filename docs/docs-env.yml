name: cb-web 
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.8
  - quarto
  - librsvg # Used by quarto to convert svg images to pdf
  - svgutils # Useful for python scripts to create grids of SVG plots
  - pyyaml
  - scipy
  - numpy
  - matplotlib
  - seaborn
  - pandas
